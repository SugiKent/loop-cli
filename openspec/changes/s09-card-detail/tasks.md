## 1. 前提と依存

- [x] 1.1 s08-queue-screen が実装済みで、`internal/ui` に `Model` / `New` / `fetchedMsg` / `Subject` / プレビュー / `Elapsed` があり `go build ./... && go test ./internal/ui/` が通ることを確認する。未実装ならこの change を始めない。s08 の最終版の `specs/queue-screen/spec.md` と、この change の `specs/queue-screen/spec.md`（MODIFIED の 2 Requirement）を突き合わせ、s08 側の本文がレビューで変わっていれば写し直す
- [x] 1.2 Bubbles v2 の viewport の使い方（作成・サイズ設定・内容の設定・1 行 / ページのスクロール・先頭に戻す）を README で確認し、spec「詳細の本文領域はスクロールし、ヘッダ領域は固定する」の振る舞いに合わせる。go.mod に新しい依存は要らない（Bubbles は s08 が追加済み）

## 2. コメントの行と depends on（画面に依存しない部分）

- [x] 2.1 s08 が `internal/ui/preview.go` に実装したコメント 1 件を行にする関数に `expanded bool` 引数を足す（例: `author string, body string, createdAt time.Time, ai bool, loc *time.Location, width int, expanded bool` を受けて `[]string` を返す。s08 の実際のシグネチャに合わせる）。s08 の処理（routine マーカー行の除去 = `strings.TrimSpace` 後の行全体一致、Glamour で幅 − 1 のレンダリング、AI の全行に `▌`）は変えない。`ai` かつ `!expanded` のときだけ見出し 1 行 `▌AI  HH:MM  <要約>  (+<n> 行)` を返す（要約はマーカー行を除いた生の `body` の空行でない最初の行、`n` はその後の行数。Glamour を通さない）。プレビュー（s08）は `expanded` を true で呼び、s08 のプレビューのテストが変更なしで通ることを確認する
- [x] 2.2 `internal/ui/detail.go` に `dependsOn(body string) []int`（`(?i)depends on #(\d+)` を本文全体から出現順に。`regexp` はパッケージ変数）を置く。`internal/ui/detail_test.go` で `集計が遅い。\n\ndepends on #12\nDepends on #34` → `[12, 34]`、無ければ空を検証する

## 3. 画面の状態と詳細のキー処理

- [x] 3.1 `internal/ui/detail.go` に画面の状態の型（キュー / カード詳細 / PR 詳細）と `detail` 構造体（`card model.Card`、`prIdx int`、`expanded bool`、`fromDetail bool`、viewport）を置き、`internal/ui/model.go` の `Model` に `screen` と `detail` を足す。`Update` の先頭で `q` / `ctrl+c`（`tea.Quit`）・端末サイズ（`width` / `height` を保持し、詳細を開いていれば viewport のサイズと内容を作り直す）・`fetchedMsg`（s08 の規則どおり `cards` / `at` / `partial` / `errText` を更新。`detail.card` は触らない）・spinner を画面に関わらず処理し、キーは `screen` で振り分ける
- [x] 3.2 キュー画面のキー処理に `Enter` を足す: 選択行があれば `detail = {card: その Card のコピー, prIdx: 0, expanded: false}`、`Issue` が nil なら `screen` を PR 詳細（`fromDetail` false）、それ以外はカード詳細にして、本文領域を作り直し viewport を先頭に戻す。選択行が無ければ `m, nil`。`g` / `x` / `Esc` はキュー画面では `m, nil` のまま
- [x] 3.3 カード詳細画面のキー処理を実装する: `Esc` → キュー、`Tab` → `prIdx = (prIdx+1) % len(PRs)`（`PRs` が空なら何もしない）、`Enter` / `g` → `PRs` が空でなければ PR 詳細（`fromDetail` true、viewport 先頭）、`x` → `expanded` を反転して本文領域を作り直す、`j` / `down` / `k` / `up` / `pgdown` / `pgup` → viewport のスクロール、それ以外（`1`〜`4` / `p` / `a` 等）→ `m, nil`。PR 詳細画面: `Esc` → `fromDetail` ならカード詳細（viewport 先頭）、そうでなければキュー、`g` → `Issue` が nil でなければカード詳細、`x` とスクロールはカード詳細と同じ、それ以外は `m, nil`
- [x] 3.4 `internal/ui/detail_test.go` で画面遷移を検証する。`example` の `Result`（`fetch.Fetch(ctx, gh.NewFake("../gh/testdata/fixtures/example"), []string{"org/app"})`、`at` は `2026-09-05T12:04:00+09:00`）を渡した `Model` で: `Enter` → `screen` がカード詳細で `detail.card.Issue.Number` が 108、続けて `Esc` → キューでタブ今やる・`cursor` 0。`Issue` nil で `docs` の open PR 60 だけの手書き Card で `Enter` → PR 詳細で対象が PR 60、`Esc` → キュー。異常タブ（0 行）で `Enter` → キューのまま・コマンド nil。カード詳細を開いた `Model` に `2` と `4` と `p` を与えると画面はカード詳細のままで、タブは今やるのまま。カード詳細を開いた後に issue 108 を含まない `Result` の `fetchedMsg` を渡すと画面はカード詳細のままで対象は 108 のままであり、`Esc` の後の今やるタブの行は新しい `Result` のものになる。カード詳細を開いた `Model` に `q` を与えると `tea.QuitMsg` を生むコマンドが返る。カード詳細を開いた `Model` に `g` と `g` と `Enter` と `Esc` を順に与えると、画面は順に PR 詳細（131）、カード詳細、PR 詳細、カード詳細になる。`Issue` が nil の Card から開いた PR 詳細で `g` を与えると画面は PR 詳細のままで、コマンドは nil。PR 3 件（131 merged、140 merged で Canonical、151 open）の手書き Card を開いて `Tab` を 3 回与えると `prIdx` は順に 1、2、0 になる。s08 の `internal/ui/model_test.go` の「未実装のキーは何も変えない」から `Enter` を外し、`x` と `Esc` を足す（`g` はキュー画面では引き続き何もしない）

## 4. 詳細の View

- [x] 4.1 `internal/ui/detail.go` にカード詳細のヘッダ領域を実装する（順序だけ固定し、行番号を前提にしない）: `<Repo> #<Number>  <Title>` / `Card.Result.Summary`（空なら省く）/ `段階: ` + `model.IssueStages(Labels)` を空白区切り（無ければ `段階なし`）+ `[blocked]` / `[wip]` / `[question]` の順で `Labels` にあるもの / `dependsOn` が 1 件以上なら `depends on: #<n> #<m> …` / PR 一覧。PR 一覧は `propose` / `apply` / `archive` の順に、`model.PRStages(Labels)` の先頭がその段階の PR を `PRs` の並び順で `[<段階>] PR#<n> <open|merged|closed>`（`Canonical` なら直後に `（最新・正本）`）+ `未確定 <n> 件` / `1 行目なし`（`model.ParseUndecided`）+ `labels: …` + `checks 緑` / `checks 緑以外` / `checks 未取得`（`classify.ChecksGreen`、nil は未取得）+ `mergeable <Mergeable>` / `mergeable 未取得` で出し、無い段階は `[<段階>] なし`、段階無しの PR は最後に `[-]` で出す。`prIdx` の PR の行の先頭に `▶`、他は同じ幅の空白。ヘッダ領域の各行（PR 詳細の 2 行も）は表示幅が `width` を超えれば Lip Gloss の切り詰めで末尾を `…` にする
- [x] 4.2 同ファイルにカード詳細の本文領域を実装する: `Issue.Body` を Glamour（幅 = `width`。s08 のプレビューと同じ呼び方。失敗なら `Body` そのまま、空なら省略）/ `model.LatestBlockedBy(Issue.Comments)` が見つかれば `blocked-by: <value>`、`value` が `human` なら `model.ParseQuestions(c.Body)` の各質問を `Q<Number>. <Title>` と `  <Letter>: <Text>`（`Recommended` なら `  <Letter>（推奨）: <Text>`）で、質問が無ければ `c.Body` から routine マーカー行と `blocked-by:` 行を除いた全行 / `Issue.Comments` が nil なら `コメント: 未取得`、長さ 0 なら `コメント: なし`、それ以外は 2.1 の関数を `expanded` と `at.Location()` で呼んで並べる
- [x] 4.3 同ファイルに PR 詳細を実装する。ヘッダ領域: `<Repo> PR#<Number>  <Title>` / `[<段階>] <状態>  labels: …`（段階は `PRStages` の先頭、無ければ `-`）。本文領域: `未確定の判断: <n> 件` / `1 行目に未確定の判断が無い` → `紐づく issue: #<n>` / `紐づく issue: なし`（`fetch.LinkedIssue(Title, Body)`）→ `MergeState` nil なら `checks: 未取得`、non-nil なら `mergeable: <Mergeable> <MergeStateStatus>` と `StatusCheckRollup` の各行（`CheckRun`: `  <Name>: <Conclusion>`（空なら `<Status>`）、`StatusContext`: `  <Context>: <State>`、0 件なら `  checks: なし`）→ `Body` の Glamour → `Comments`（nil `コメント: 未取得` / 空 `コメント: なし` / 2.1 の関数）→ `ReviewThreads`（nil `review thread: 未取得` / 空 `review thread: なし` / `IsResolved` false を先に true を後に `sort.SliceStable` で並べ、各 thread を `thread 未 resolve` / `thread resolved` の見出しと、各コメントを 2.1 の関数に `Author.Login` / `Body` / `CreatedAt` / `model.IsAI(Body)` / `expanded` true で渡した行）
- [x] 4.4 `internal/ui/view.go` の `View` を `screen` で振り分ける。詳細（両画面）はヘッダ領域 / `─` の区切り線 / viewport の `View` / フッタを縦に並べ、幅は `width`、本文領域の高さは `height - ヘッダ領域の行数 - 2` で下限 1。1 を割るときはカード詳細の PR 一覧の行を末尾から落として 1 を確保する。ヘッダ領域の各行は 4.1 のとおり幅で切り詰めて `…` を付ける。フッタの左はカード詳細 `Esc 戻る  Tab PR 選択  Enter PR を開く  x 展開  g PR へ  j/k スクロール  q 終了`（`PRs` が空なら `Tab PR 選択  Enter PR を開く  g PR へ` を省く）、PR 詳細 `Esc 戻る  x 展開  g issue へ  j/k スクロール  q 終了`、右は s08 と同じステータス。キュー画面のフッタの左を `j/k 移動  1-4/Tab タブ  Enter 開く  q 終了`（1 ペインでは s08 どおり `p …` を追加）に変える。s08 の `view_test.go` の「`Enter 開く` を含まない」検証を「含む」に直し、`Esc 戻る` を含まないことを足す
- [x] 4.5 `internal/ui/detail_test.go` で `View`（ANSI を除いた文字列）を検証する。幅 120・高さ 40 のサイズメッセージを与えた `Model` で `example` の issue 108 のカード詳細: `org/app #108`、issue 108 の `Title`、`PR #131 の質問に答える`（`Summary`）、`段階: stage:propose`、`[question]` があり `[blocked]` / `[wip]` / `depends on:` が無い。`▶` で始まり `[propose] PR#131 open`、`1 行目なし`、`labels: propose question`、`checks 未取得`、`mergeable 未取得` をこの順で含む行、`[apply] なし`、`[archive] なし`。本文の `認証まわりの仕様を決めたい`、`blocked-by:` 無し、`コメント: 未取得` / `コメント: なし` 無し。`▌AI  18:00  Q1: セッションの寿命は何日にしますか。  (+1 行)` の行、`Q2: 失効時は` 無し、`user-2  18:12` の行と `寿命は 30 日で。` の行がどちらも `▌` で始まらない。`x` 後: `▌AI  18:00` の行と `▌` で始まり `Q2: 失効時はログイン画面へ戻しますか。` を含む行、`<!-- routine -->` と `(+1 行)` 無し。もう一度 `x`: 元に戻る。`x` → `Esc` → `Enter`: `(+1 行)` あり。issue 140: `起動時に設定ファイルが無いと落ちる` と `コメント: 未取得` があり `▌` 無し。手書き: `Labels` 空・`Body` `集計が遅い。\n\ndepends on #12\nDepends on #34` → `段階なし` と `depends on: #12 #34`。`stage:propose` + `stage:apply` + `blocked` + `wip` → `段階: stage:propose stage:apply`、`[blocked]`、`[wip]` の順。`blocked` + `question` + AI コメント `<!-- routine -->\nblocked-by: human\n## Q1. 名前での絞り込みを含めるか\n- 選択肢 A（推奨）: 含めない\n- 選択肢 B: 含める` → `blocked-by: human`、`Q1. 名前での絞り込みを含めるか`、`A（推奨）: 含めない`、`B: 含める` の順。同じで `Body` `<!-- routine -->\nblocked-by: human\n次の方針をコメントで教えてください` → `blocked-by: human` と `次の方針をコメントで教えてください`。PR 3 件（131 は merged、140 は merged で Canonical、151 は open で本文 1 行目が `未確定の判断: 0 件`、`MergeState` が `MERGEABLE` と `CheckRun test SUCCESS`）の手書き Card を開くと、`[propose] PR#131 merged` の行（`（最新・正本）` を含まない）、`[propose] PR#140 merged（最新・正本）` の行、`[apply] PR#151 open` と `未確定 0 件` と `checks 緑` と `mergeable MERGEABLE` を含む行、`[archive] なし` の行がこの順で出る（幅 120）。`Tab` を 3 回与えると `▶` の行は順に `PR#140`、`PR#151`、`PR#131` になる。`apply` の PR 90 とラベル無しの PR 61 を持つ Card を開くと `[propose] なし`、`[apply] PR#90 open`、`[archive] なし`、`[-] PR#61 open` がこの順で出る
- [x] 4.6 同ファイルで PR 詳細の `View` を検証する。`example` のカード詳細で `Enter`: `org/app PR#131`、`[propose] open  labels: propose question`、`1 行目に未確定の判断が無い`、`紐づく issue: #108`、`checks: 未取得`、`issue #108 の提案`、`▌AI  19:31  Q1: マイグレーションを分けますか。  (+0 行)`、`review thread: 未取得` の順。手書きの `apply` PR（`ReviewThreads` が resolved（`user-3` `直しました`）→ 未 resolve（`user-1` `<!-- routine -->\nこの分岐は残しますか`）の順、`MergeState{MERGEABLE, CLEAN, [CheckRun test SUCCESS, StatusContext ci/legacy PENDING]}`）→ `mergeable: MERGEABLE CLEAN`、`test: SUCCESS`、`ci/legacy: PENDING` の順、`thread 未 resolve` が `thread resolved` より前、`この分岐は残しますか` の行は `▌` で始まり `直しました` の行は始まらない。`MergeState{UNKNOWN, []}` + `ReviewThreads` 空 + `Comments` 空 → `mergeable: UNKNOWN`、`checks: なし`、`コメント: なし`、`review thread: なし`。`Comments` が `user-2` の `PR #131 の評価です。\n\n## PR リスク評価\n\n- 影響範囲: 小`（`model.CommentFrom` で `AI` true）1 件の PR → `▌AI` で始まり `PR #131 の評価です。` と `(+4 行)` を含む行があり `影響範囲: 小` は無い
- [x] 4.7 同ファイルでスクロールとフッタを検証する。`Body` が `行01`〜`行60` の 60 段落（空行区切り）の issue と open PR 1 件の手書き Card を幅 100・高さ 20 の `Model` で開く: `View` に `org/app #<n>` と `行01` があり `行30` が無い。`j` × 5 後: ヘッダ行があり `行01` が無く `行06` がある。続けて `Enter` → `Esc` 後: `行01` がある。フッタ: カード詳細の最終行に `Esc 戻る` / `Tab PR 選択` / `x 展開` / `q 終了` があり `1-4/Tab タブ` が無い。PR 詳細の最終行に `Esc 戻る` と `g issue へ` があり `Tab PR 選択` が無い。幅 60・高さ 40 で `example` のカード詳細: `org/app #108`、`PR#131`、`認証まわりの仕様を決めたい` が全部あり `p プレビュー` が無い。issue 140（`PRs` 空）のカード詳細の最終行に `Esc 戻る` と `x 展開` があり `Tab PR 選択` / `Enter PR を開く` / `g PR へ` が無い。幅 40 で `Title` の表示幅が 60 の issue のカード詳細: `org/app #` で始まる行の表示幅が 40 以下で末尾が `…`。幅 120・高さ 7 で `Body` `行01` と `propose` / `apply` / `archive` の open PR 3 件の Card: `org/app #<n>`、`行01`、`q 終了` があり `[archive] PR#` の行が無い

## 5. 最終確認

- [ ] 5.1 手動確認: 稼働リポジトリ 1 件を設定した状態（V-2）で起動し、`Enter` でカード詳細が開き、AI コメントが `▌` 付きで畳まれて `x` で開き、`Tab` で PR の選択が動いて `Enter` で PR 詳細に入り、`g` と `Esc` で戻れ、`j` / `k` で本文がスクロールし、端末を狭くしても詳細が 1 ペインで出ることを確認する
- [x] 5.2 `gofmt -l .` が空で、`go build ./... && go vet ./... && go test ./...` が通ることを確認する（`golangci-lint run ./...` も通す）。`internal/fetch` / `internal/classify` / `internal/gh` / `cmd/sugi-loop-cli` の既存テストが変更なしで通ることを確認する
