## 1. 前提と依存

- [x] 1.1 s08-queue-screen が archive 済みで `internal/ui` の `Model` / `View` / `tableLines` と `cmd/sugi-loop/main.go` の `run()` があることを確認する。着手時点の `ui.New` の署名（s10 が `New(fetcher, client, editor)` に変える）と `run()` の中身を見て、この change のテストと配線をそれに合わせる。`openspec/changes/s10-answer-question/specs/tui-entrypoint/spec.md` の「1 フレーム描画」ブロックと、この change の MODIFIED を突き合わせ、s10 の本文がレビューで変わっていれば、手順 1 の変更と Scenario「設定ファイルが無い端末ではフォームの後にキュー画面が出る」の追加だけを保って写し直す（archive の順は s10 → s08a）。`internal/gh/client.go` の `Check` が `exec.LookPath` のエラーを `%w` で包んでいることを確認する
- [x] 1.2 `go get charm.land/huh/v2@latest` で go.mod / go.sum に依存を加える。README で複数行入力（Text）・select・confirm・1 行入力（Input）の型名、検証関数の付け方、既定値の入れ方、利用者の中止を表すエラー値の名前、フォームを標準入出力で実行する関数を確認し、spec の振る舞いに合わせる。標準入力が端末かの判定関数（`github.com/charmbracelet/x/term` の `IsTerminal`。go.sum に無ければ `golang.org/x/term`）も確認する

## 2. internal/config（検証規則の公開）

- [x] 2.1 `internal/config/config.go` の `isOwnerName` を `IsRepoName(name string) bool` に改名して公開し、`validate` はそれを呼ぶ。規則（`/` がちょうど 1 つ、`owner` と `name` が空でない、空白とタブを含まない）は変えない
- [x] 2.2 `internal/config/config_test.go` に `IsRepoName` の検証を足す: `org/app` → true。`app` / `org/app/extra` / `/app` / `org/` / `org/ app` → false。既存の `TestLoadInvalidConfig` が変更なしで通ることを確認する

## 3. internal/onboarding（解析・YAML・書き出し・フォーム）

- [x] 3.1 `internal/onboarding/onboarding.go` に `Answers{Repos []string; MergeMethod config.MergeMethod; Notify bool; Editor string}`、`ErrAborted`（`errors.New("設定の作成を中止しました")`）、`ParseRepos(text string) ([]string, error)`（`strings.Split(text, "\n")` の各行を `strings.TrimSpace` して空行を捨てる。0 行なら `repos を 1 件以上入力してください`、`config.IsRepoName` が false の行があれば `<行>: owner/name 形式ではありません`）、`Marshal(a Answers) ([]byte, error)`（`repos` / `refresh_interval_sec`（常に 120）/ `merge_method` / `editor` / `notify` の順にフィールドを持つ非公開の構造体を `go.yaml.in/yaml/v3` の Encoder にインデント 2 で書く）、`Write(path string, a Answers) error`（`os.MkdirAll(filepath.Dir(path), 0o700)` → `os.WriteFile(path, data, 0o600)`）を実装する
- [x] 3.2 `internal/onboarding/onboarding_test.go` で検証する: `ParseRepos("org/app\n\n  org/web  \n")` → `[org/app org/web]`。`ParseRepos("\n  \n")` → エラーに `repos`。`ParseRepos("org/app\napp\n")` / `("org/app/extra")` / `("org/ app")` → エラーにそれぞれ `app` / `org/app/extra` / `org/ app` と `owner/name`。`Marshal(Answers{Repos: {org/app, org/web}, MergeMethod: squash, Notify: true, Editor: "$EDITOR"})` が spec「mvp.md の設定例と同じ YAML になる」の文字列（末尾に改行 1 つ）とバイト単位で等しい。`t.Setenv("EDITOR", "nvim")` で `Answers{Repos: {org/app}, MergeMethod: rebase, Notify: false, Editor: "$EDITOR"}` を `filepath.Join(t.TempDir(), "sugi-loop", "config.yml")` に `Write` → エラー無し、ファイルが存在、`config.Load` の結果が `Repos` `[{org/app rebase}]`、`MergeMethod` rebase、`RefreshIntervalSec` 120、`Notify` false、`Editor` nvim。`Editor: ""` の `Marshal` を一時ファイルに書いて `config.Load` → エラー無しで `Editor` 空
- [x] 3.3 `internal/onboarding/form.go` に `Run(path string) error` を実装する: huh のフォームに 4 項目を順に置く。`repos` は複数行入力（タイトル `repos`、説明 `owner/name を 1 行に 1 つ`、プレースホルダ `org/app\norg/web`、検証は `ParseRepos` のエラーをそのまま返す）。`merge_method` は select（`squash` / `merge` / `rebase`。値は `config.MergeSquash` 等、既定 `squash`）。`notify` は confirm（既定 true）。`editor` は 1 行入力（既定 `$EDITOR`）。フォームの実行が利用者の中止を表すエラーで終わったら `ErrAborted` を返し、他のエラーはそのまま返す。完了したら `ParseRepos` の結果と各値で `Answers` を組み `Write(path, a)` を返す。`Run` は `internal/ui` を import しない

## 4. cmd/sugi-loop（判定順と Check 失敗の文言）

- [x] 4.1 `cmd/sugi-loop/main.go` に `ensureConfig(path string, isTerminal bool, form func(path string) error) error` を実装する: `os.Stat(path)` が nil → nil。`errors.Is(err, fs.ErrNotExist)` → `isTerminal` が false なら `fmt.Errorf("設定ファイルがありません: %s", path)`、true なら `form(path)` の返り値を返す（`errors.Is(err, onboarding.ErrAborted)` なら `fmt.Errorf("設定の作成を中止しました（%s は書いていません）", path)` に写す）。それ以外の `Stat` エラー → `fmt.Errorf("config %s: %w", path, err)`。`checkError(err error) error` を実装する: `errors.Is(err, exec.ErrNotFound)` → `gh が見つかりません\nhttps://cli.github.com/ から GitHub CLI をインストールしてください`。`errors.As(err, &ghErr)`（`*gh.Error`）→ `gh の認証に失敗しました: <TrimSpace(Stderr)>\ngh auth login を実行してください`。それ以外 → `err` をそのまま。`run()` を `DefaultPath` → `ensureConfig(path, 標準入力が端末か, onboarding.Run)` → `Load` → `NewClient` → `if err := client.Check(ctx); err != nil { return checkError(err) }` → `ui.New`（着手時点の署名）→ プログラム実行の順にする。`main()` は変えない
- [x] 4.2 `cmd/sugi-loop/main_test.go` で検証する: 一時ディレクトリに `repos:\n  - org/app\n` を置いたパス・true・呼ばれた回数を数えるスタブ → nil でスタブ 0 回。`repos: [org/app\n` を置いたパス → nil でスタブ 0 回、続けて `config.Load` がパスを含むエラー、ファイルの内容は不変。存在しないパス・true・スタブ → スタブが同じパスで 1 回呼ばれ、スタブの返り値（`errors.New("x")`）がそのまま返る。存在しないパス・false・スタブ → スタブは 0 回呼ばれ、返るエラーは `設定ファイルがありません` とそのパスを含む。存在しないパス・true・`ErrAborted` を返すスタブ → エラーに `設定の作成を中止しました` とパス。`checkError(fmt.Errorf("gh が見つかりません: %w", exec.ErrNotFound))` → 文字列を `\n` で分けると 2 行で、1 行目に `gh が見つかりません`、2 行目に `https://cli.github.com/`。`checkError(&gh.Error{Args: {auth, status}, ExitCode: 1, Stderr: "X Failed to log in to github.com using token (GH_TOKEN)\n"})` → 2 行で、1 行目に `gh の認証に失敗しました` と `X Failed to log in to github.com using token (GH_TOKEN)`、2 行目に `gh auth login`。`checkError(fmt.Errorf("gh auth status: %w", context.DeadlineExceeded))` → 改行を含まず元の文字列を含む

## 5. internal/ui（空キューのヒント）

- [x] 5.1 `internal/ui/view.go` の `tableLines(h int)` に 0 件分岐を足す: `len(m.cards) == 0 && !m.fetching && m.errText == ""` なら、`stage:* ラベルの無いリポジトリは何も出ません。` と `issue-driven-sdd の routines-setup を回したリポジトリを設定してください` の 2 行を、上に `(h - 2) / 2` 行（負なら 0）の空行を置き、各行の左に `(m.width - 表示幅) / 2`（負なら 0）の空白を置いて返し、`cut(lines, h)` で切る。右には空白を足さない（Lip Gloss の配置関数は右詰めの空白を足すので使わない）。行が端末幅を超えれば `ansi.Truncate` で切る。それ以外は従来どおり
- [x] 5.2 `internal/ui/view_test.go` に `plainText` / `plain` を使って検証を足す: `fetchedMsg{res: &fetch.Result{}, at: at}` を渡した `Model` → 2 行の文言を含み、ヘッダに `[1]今やる 0`、フッタに `q 終了`。`New(...)` 直後 → `routines-setup` を含まずフッタに `取得中`。`New(...)` 直後に `fetchedMsg{err: errors.New("search issues: gh search issues: exit 1: rate limited")}` → `routines-setup` を含まずフッタに `rate limited`。`exampleResult(t)` → `routines-setup` を含まず `PR131` の行がある。`&fetch.Result{Errors: []error{errors.New("ViewPR org/app#131: open pr-131.json: no such file")}}` → 2 行の文言を含み、フッタに `詳細取得の失敗 1 件`。`Cards` 空の `Result` と `tea.WindowSizeMsg{Width: 120, Height: 40}` → `routines-setup` を含む行の先頭の空白数が `(120 - ansi.StringWidth(strings.TrimSpace(行))) / 2` で、行末に空白が無い。幅 60 の 1 ペインで `p` を押してプレビューを出した状態 → `routines-setup` を含まない。s08 / s09 の既存テストが変更なしで通ることを確認する

## 6. 手動確認と最終確認

- [x] 6.1 手動確認: `HOME` を一時ディレクトリにして `go run ./cmd/sugi-loop` を端末で起動し、フォームが出て `repos` に `owner/name` でない行を入れると先に進めず、正しい値で完了すると `$HOME/.config/sugi-loop/config.yml` が spec の形式で作られてキュー画面に進むこと。もう一度起動するとフォームが出ないこと。設定ファイルを `repos: [org/app` に壊して起動するとフォームが出ずパスと原因が出て exit 1 で、ファイルが不変であること。フォームで Ctrl+C を押すと `設定の作成を中止しました` が出て exit 1 でファイルが無いこと。`go run ./cmd/sugi-loop < /dev/null` で `設定ファイルがありません: <パス>` が出て exit 1 であること。`PATH` から `gh` を外して起動すると 2 行（`gh が見つかりません` / `https://cli.github.com/`）が出ること。`GH_TOKEN=invalid` で起動すると `gh の認証に失敗しました` と `gh auth login` の 2 行が出ること。`stage:*` ラベルの無いリポジトリだけを設定して起動すると表の領域の中央にヒントが出ること（V-2 の稼働リポジトリを設定すればヒントが出ないこと）
- [x] 6.2 `gofmt -l .` が空で、`go build ./... && go vet ./... && go test ./...` が通ることを確認する（`golangci-lint run ./...` も通す）。`internal/fetch` / `internal/classify` / `internal/gh` / `internal/model` / `cmd/sugi-loop-cli` の既存テストが変更なしで通ることを確認する
