# tasks: 2026-09-10-s26-wrap-titles

Refs #2

## 0. 前提

- [ ] 0.1 `proposal.md` の `## 未確定の判断` が空であることを確認する（Q1 の回答が入ると、キュー画面のタスク群がここに足される）

## 1. 折り返しヘルパー

- [ ] 1.1 `internal/ui` に `wrapTitle(prefix, title string, width int) []string` を足す。design.md D1〜D3 のとおり `ansi.Hardwrap` で折り、2 行目以降を `prefix` の表示幅ぶん字下げし、字下げの余地が 1 列未満なら字下げを付けない
- [ ] 1.2 `internal/ui/detail_test.go`（または新設の `wrap_test.go`）に `wrapTitle` のテストを書き、幅に収まるタイトルが 1 行のまま返ること・全角 40 字のタイトルが幅 40 でどの行も表示幅 40 以下になること・2 行目以降の行頭が `prefix` の表示幅と同じ空白であること・`width - StringWidth(prefix) < 1` の幅で字下げが付かないことを検証する

## 2. ヘッダへの適用

- [ ] 2.1 `cardHeaderLines` の 1 行目を `wrapTitle` の戻り値に差し替え、Scenario「長いタイトルは折り返して全文出す」「全角文字のタイトルは表示幅で折り返す」がテストで通ることを確認する
- [ ] 2.2 `prHeaderLines` の 1 行目を `wrapTitle` の戻り値に差し替え、Scenario「PR 詳細の長いタイトルも折り返す」がテストで通ることを確認する
- [ ] 2.3 既存の「長いヘッダ行は幅で切り詰める」テストを `Card.Result.Summary` を対象に書き換え、1 行目以外のヘッダ行が今までどおり `…` で切れることを検証する

## 3. 高さの上限

- [ ] 3.1 `detailHeader` に design.md D4 の規則を足す。PR 一覧を全部落としても本文 1 行を確保できないとき、折り返したタイトルの行を末尾から落とし、残った最後のタイトル行を `…` 付きに切る（1 行目は必ず残す）
- [ ] 3.2 Scenario「折り返したタイトルが高さを埋めても画面は端末の高さに収まる」のテストを書き、幅 40・高さ 8 で `View` の行数がちょうど 8 になり、フッタとタイトル 1 行目が残ることを検証する
- [ ] 3.3 既存の Scenario「低い端末では PR 一覧を削って本文 1 行を残す」と「狭い端末でも詳細は 1 ペインで全部出す」のテストが通ったままであることを確認する

## 4. 仕上げ

- [ ] 4.1 `openspec validate 2026-09-10-s26-wrap-titles --strict` が通ることを確認する
- [ ] 4.2 `go build ./... && go vet ./... && go test ./...` が通ることを確認する
