## 1. 紐づけのパース

- [x] 1.1 `internal/fetch/link.go` を作成し、`LinkedIssue(title, body string) (int, bool)` を実装する。title は先頭の空白を除いて `^\[(propose|apply|archive)\]\s*#(\d+)` で見る。当たらなければ本文を `(?i)(^|[^A-Za-z0-9])(refs|closes)\s+#(\d+)` で先頭から探し、最初の番号を返す。どちらも無ければ `0, false`。`regexp` はパッケージ変数にコンパイルする
- [x] 1.2 `internal/fetch/link_test.go` を作成し、spec `card-fetch`「PR は title と本文のパースで同一リポジトリの Issue に紐づく」の Scenario を検証する: title `[propose] #108 …` → 108 / 本文 `Closes #108` → 108 / 本文 `Refs #48` → 48 / title `[apply] #48` と本文 `Refs #12\nCloses #48` → 48（title 優先）/ 本文 `#108 と同じ問題` → `0, false`。加えて、小文字の本文 `closes #7` からは 7 を返すこと、本文 `xRefs #7` と title `[WIP] #7`（本文無し）からはどちらも `0, false` を返すことを検証する
- [x] 1.3 `internal/gh/fake.go` の `Fake` に `mu sync.Mutex` を足し、`Calls` に追記する 9 か所（`ViewIssue` と書き込み 8 メソッド）を `f.mu.Lock()` / `defer f.mu.Unlock()` で守る。`Fetch` が `ViewIssue` を並行して呼ぶため。`internal/gh/fake_test.go` に spec `gh-fake`「Fake は並行呼び出しでも Calls の内容を壊さない」の Scenario を足す: `example` の `Fake` に対して 8 本のゴルーチンから `ViewIssue(ctx, "org/app", 108)` を同時に呼び、`Calls` が 8 件で全件 `Method` が `ViewIssue`・`Number` が 108 であること。`internal/gh` の既存テストが変更なしで通り、`go test -race ./internal/gh/` が通ることを確認する

## 2. Fetch 本体

- [x] 2.1 `internal/fetch/fetch.go` を作成し、`const CallTimeout = 30 * time.Second`、`type Result struct { Cards []model.Card; Errors []error }`、`func Fetch(ctx context.Context, client gh.GHClient, repos []string) (*Result, error)` を実装する。手順: (1) `CallTimeout` 付きの子 `ctx` で `SearchIssues` → 失敗なら `fmt.Errorf("search issues: %w", err)` を返す。(2) 同様に `SearchPRs` → `search prs: `。(3) `model.IssueFromSearch` / `model.PRFromSearch` で写す。(4) 詳細取得の対象を決め（issue: `HasLabel(question)` → `ViewIssue`。PR: 全件 → `ViewPR`、`len(PRStages) >= 1 && !HasLabel(question) && ParseUndecided(Body) == (0, true)` → `ViewPRMergeState`、`HasLabel(apply)` → `ReviewThreads`）、`sync.WaitGroup` + 容量 4 のチャネルで並行実行する（上限 4 と search の直列は design.md の既定値）。各呼び出しは `context.WithTimeout(ctx, CallTimeout)` の子 `ctx` で行い、結果を自分のインデックスの `Comments`（`model.CommentFrom` で写す）/ `MergeState` / `ReviewThreads` に入れる。失敗は `sync.Mutex` で守った列に `fmt.Errorf("<Method> <repo>#<n>: %w", err)` として積む。(5) 全ゴルーチン終了後に親 `ctx.Err() != nil` なら `fmt.Errorf("fetch: %w", ctx.Err())` を返す。(6) `map[string]map[int]int`（repo → 番号 → issue のインデックス）を作り、各 PR を `LinkedIssue(Title, Body)` で引いて同じ `Repo` の issue があればその Card の `PRs` に追加、無ければ `Issue` nil の Card を作る。(7) 各 Card の `PRs` を段階順（`PRStages` の先頭が propose → apply → archive、無しは末尾）→ 番号昇順に `sort.SliceStable` で並べる。(8) `Cards` は issue カードを search 順、続けて PR 単独カードを search 順に並べ、各 Card を `classify.Card` に通す。(9) `Errors` を issue のエラーを (repo, number) 順、続けて PR のエラーを (repo, number) 順に整列して `Result` を返す
- [x] 2.2 `internal/fetch/testdata/` に design.md の表どおりの手書き fixture を作る。全 open PR が `ViewPR` を呼ぶので、`partial` の PR 131 を除き `search-prs.json` の全 PR に `pr-<n>.json` を置く（`comments` は明示する。末尾が人だと s05 の規則 2 で進行中になる）。`link/` には、`search-issues.json` に issue 108（`stage:propose`）を、`search-prs.json` に次の PR をこの順で置く（段階順ソートを検証するため 151、131、140 の順）: PR 151（`archive`、title `[archive] #108`、本文 1 行目 `未確定の判断: 0 件`、`isDraft: true`）、PR 131（`propose`、本文 `Closes #108`）、PR 140（`apply`、title `[apply] #108`、本文 1 行目 `未確定の判断: 1 件`）、PR 60（`docs`）、PR 61（ラベル無し）、PR 62（ラベル無し、title `[propose] #999`）、PR 132（`propose`、title `propose: 検索の仕様`（`[propose] #n` 形式ではない）、本文 1 行目 `未確定の判断: 2 件`、`Refs` / `Closes` 無し）、PR 90（`apply`、title `apply: 検索の実装`、本文 1 行目 `未確定の判断: 1 件`、`Refs` / `Closes` 無し）。詳細は `pr-151.json`（`mergeable: MERGEABLE`、`statusCheckRollup` に `CheckRun` の `SUCCESS` 1 件、`comments` 空）、`pr-132.json`（`mergeable: MERGEABLE`、AI のコメント 1 件）、`pr-90.json`（コメント 2 件、末尾が人）、`pr-131.json` / `pr-140.json` / `pr-60.json` / `pr-61.json` / `pr-62.json`（`mergeable: UNKNOWN`、`comments` 空）、`pr-140-review-threads.json` と `pr-90-review-threads.json`（どちらも `isResolved: true` の thread 1 件）を置く。`pr-151-review-threads.json` 等の他の review threads は置かない。`multirepo/` には `org/app` の issue 12 と `org/web` の issue 12（どちらもラベル無し）、`org/web` の PR 30（title `[propose] #12`、`propose`、本文 1 行目 `未確定の判断: 1 件`）と `pr-30.json`（`comments` 空）を置く。`samestage/` には issue 108（`stage:propose` + `question`）と `issue-108.json`（コメント 2 件、末尾が人）を置き、`search-prs.json` には PR 140（`propose`、`未確定の判断: 1 件`、`Closes #108`）、PR 131（`propose` + `question`、`Closes #108`）、PR 88（`apply` + `question`）をこの順で置く。詳細は `pr-140.json`（`comments` 空）、`pr-131.json`（`<!-- routine -->` 始まりのコメント 1 件）、`pr-88.json`（AI のコメント 1 件）、`pr-88-review-threads.json`（末尾が AI の未 resolve thread 1 件）を置く。`partial/` には issue 108（`question`）と `issue-108.json`、PR 131（`propose` + `question`、`Closes #108`）を置き、`pr-131.json` は置かない。`nosearch/` には `search-prs.json` だけを置く。`login` は `user-1` / `user-2` の形式、リポジトリは `org/app`（`multirepo` は `org/web` も）、JSON の形は `internal/gh/testdata/fixtures/example/` に合わせる
- [x] 2.3 `internal/fetch/fetch_test.go` を作成し、`gh.NewFake` で次を検証する。`example`（`../gh/testdata/fixtures/example`）: Card 2 枚（108 に PR 131、140 は PR 無し）、`Card.Result.Situation` が `A` / `E`、`Errors` 空、`Fake.Calls` の `ViewIssue` が 108 の 1 件だけ、issue 108 の `Comments` 2 件、issue 140 の `Comments` nil、PR 131 の `Comments` あり・`MergeState` nil・`ReviewThreads` nil。`link`: issue 108 の `PRs` が 131 / 140 / 151 の順（`search-prs.json` は 151、131、140 の順）、PR 151 の `MergeState.Mergeable` が `MERGEABLE`・`Comments` 空・`ReviewThreads` nil で `Result.Situation` `C`、PR 140 の `MergeState` nil・`ReviewThreads` 1 件・`Situation` `other`、PR 60 / 61 / 62 / 132 / 90 が `Issue` nil の単独 Card で `Situation` が `G` / `other` / `other` / `other` / `in-progress`、PR 132 の `Comments` 1 件・`MergeState` nil、PR 90 の `Comments` 2 件・`ReviewThreads` 1 件・`MergeState` nil、`Errors` 空。`multirepo`: Card が 2 枚あり、`org/web` の issue 12 の `PRs` は PR 30 の 1 件で、`org/app` の issue 12 の `PRs` は空である。`samestage`: issue 108 の `PRs` が 131、140 の順で `Canonical` false、`Card.Result.Situation` `A`、PR 88 の `Comments` と `ReviewThreads` が両方あり `MergeState` nil。`partial`: エラー無し、PR 131 の `Comments` nil で `Situation` `other`、`Errors` 1 件で文字列に `ViewPR` / `org/app` / `131` / `pr-131.json` を含む、issue 108 の `Comments` あり。`nosearch`: エラー文字列に `search issues` と `search-issues.json` を含み `Result` nil
- [x] 2.4 同じテストファイルに `gh.GHClient` を埋め込んだ stub を置き、次を検証する。呼び出し回数: `SearchIssues` / `SearchPRs` を数えて空の結果を返す stub で `Fetch` → どちらも 1 回。親の期限: `SearchIssues` が `<-ctx.Done()` の後 `ctx.Err()` を返す stub に 50 ミリ秒の `context.WithTimeout` で `Fetch` → `errors.Is(err, context.DeadlineExceeded)`。キャンセル: `SearchIssues` は `question` 付き issue 1 件を返し、`SearchPRs` は空を返す前に stub の中で `cancel()` を呼び、`ViewIssue` は `<-ctx.Done()` の後 `ctx.Err()` を返す stub で `Fetch` → `errors.Is(err, context.Canceled)` で `Result` nil（`cancel()` を stub の `SearchPRs` 内で呼ぶことで、詳細取得に入る直前に確実にキャンセルされた状態を作る）
- [x] 2.5 同じテストファイルに `../gh/testdata/fixtures/` 直下の全ディレクトリを走査する不変条件テストを置く: 各 alias で `Fetch` がエラー無し・`Errors` 空で返り、`search-issues.json` の各番号が `Cards[].Issue.Number` にちょうど 1 回、`search-prs.json` の各番号が `Cards[].PRs[].Number` にちょうど 1 回現れる。`Situation` は見ない（現在の alias は `example` のみ。s04 で採取した alias があれば、全 open PR の `pr-<n>.json` が揃っていることを含めてそのまま通ることを確認する）

## 3. 最終確認

- [x] 3.1 `gofmt -l .` が空で、`go build ./... && go vet ./... && go test ./...` が通ることを確認する（`golangci-lint run ./...` も s01 の CI が回すので通す）。`internal/gh` と `internal/classify` の既存テストが変更なしで通ることを確認する（`example` を変えていない）
