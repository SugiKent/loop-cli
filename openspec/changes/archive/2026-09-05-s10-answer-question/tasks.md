## 1. 前提と依存

- [x] 1.1 s08-queue-screen と s09-card-detail が実装済みで、`internal/ui` に `Model` / `New(fetcher)` / `Subject` / 画面の状態（キュー / カード詳細 / PR 詳細）/ 詳細の対象（`detail.card`）があり `go build ./... && go test ./internal/ui/` が通ることを確認する。未実装ならこの change を始めない。s08 / s09 の最終版の specs と、この change の MODIFIED（`queue-screen` 3 件、`card-detail` 2 件、`tui-entrypoint` 1 件）を突き合わせ、本文がレビューで変わっていれば写し直す
- [x] 1.2 `charm.land/bubbletea/v2` の `exec.go` を読み、外部プロセス実行（`*exec.Cmd` と、終了時のエラーからメッセージを作るコールバックを受け取り `tea.Cmd` を返す関数）の名前と使い方を確認する。新しい依存は要らない

## 2. internal/action（書き込みの不変条件とテンプレート）

- [x] 2.1 `internal/action/action.go` に `Target{Repo string; Number int; IsPR bool}`、エラー値 `ErrEmptyBody` / `ErrRoutineMarker`、`BlockedByLines(body string) []string`（`strings.Split(body, "\n")` の各行を `strings.TrimSpace` してから `strings.HasPrefix(line, "blocked-by:")`。s05 `internal/model/parse.go` の `LatestBlockedBy` と同じ。該当行を `TrimSpace` 後の形で出現順に返す）、`Comment(ctx context.Context, client gh.GHClient, t Target, body string) error`（`strings.TrimSpace(body) == ""` → `ErrEmptyBody`、`HasRoutineMarker(body string) bool`（`strings.Contains(body, "<!-- routine -->") || strings.Contains(body, "&lt;!-- routine --&gt;")`）が真 → `ErrRoutineMarker`、`t.IsPR` → `client.CommentPR(ctx, t.Repo, t.Number, body)`、それ以外 → `client.CommentIssue(...)`。`AddLabel` / `RemoveLabel` は呼ばない）を実装する。マーカーの 2 文字列は `model.IsAI` が見る形と同じにする
- [x] 2.2 `internal/action/action_test.go` で `gh.NewFake("../gh/testdata/fixtures/example")` を `client` にして検証する: PR 131 への `Q1: A\nQ2: B` → `Calls` 1 件 `CommentPR` / `org/app` / 131 / `Q1: A\nQ2: B`。issue 108 への `Q1: A` → `CommentIssue` / 108。両方の後で `Calls` に `AddLabel` / `RemoveLabel` が無い。` \n\t\n` → `errors.Is(err, ErrEmptyBody)` と `Calls` 空。`<!-- routine -->\nQ1: A` / `routine のコメントは <!-- routine --> で始まるはずでは？` / `&lt;!-- routine --&gt;\nQ1: A` → `ErrRoutineMarker` と `Calls` 空。`blocked-by: human\nQ1: A` → nil で `Calls` 1 件 `CommentIssue`（拒否しない）。`BlockedByLines("Q1: A\n  blocked-by: human\nQ2: B\nblocked-by: #12")` → `[blocked-by: human, blocked-by: #12]`、`Q1: A\n> blocked-by: human を引用します\nQ2: B` → 空
- [x] 2.3 `internal/action/template.go` に `AnswerTemplate(comments []model.Comment) string` を実装する: 末尾から先頭へ見て最初の `AI` true のコメントを選び（無ければ `""`）、`model.ParseQuestions(c.Body)` の各 `Question` について `Recommended` の最初の `Option` の `Letter`、無ければ `Options[0].Letter`、無ければ `""` を `Q<Number>: <Letter>` にして `strings.Join(lines, "\n")` で返す（質問 0 件なら `""`）
- [x] 2.4 `internal/action/template_test.go` でテンプレートを検証する。`AI` が true で本文が mvp.md の 2 問（`## Q1. …\n- 選択肢 A（推奨）: 含めない。次の issue に回す\n- 選択肢 B: 含める\n## Q2. …\n- 選択肢 A: 今回は触らない\n- 選択肢 B（推奨）: 幅だけ直す`）のコメント 1 件を渡すと `Q1: A\nQ2: B` が返る。`## Q1. 方式をどうするか\n- 選択肢 A: 案 1\n- 選択肢 B: 案 2\n## Q2. 期限はいつか` → `Q1: A\nQ2: `。`example` の `issue-108.json` の 2 件（`gh.NewFake(...).ViewIssue` → `model.CommentFrom`）の後ろに `AI` true の `## Q1. 方式をどうするか\n- 選択肢 B（推奨）: 案 2` と `AI` false の `## Q1. 人が書いた見出し\n- 選択肢 A（推奨）: x` を足した列 → `Q1: B`。`example` の `pr-131.json` のコメント 1 件（`Q1: マイグレーションを分けますか。` 形式）→ `""`。nil と `AI` false だけの列 → `""`

## 3. internal/ui（エディタ・a のフロー・確認画面・投稿）

- [x] 3.1 `internal/ui/editor.go` に `type Editor func(initial string) tea.Cmd`、非公開の `editedMsg{text string; err error}`、`ExternalEditor(command string) Editor` を実装する。`strings.TrimSpace(command) == ""` なら `func() tea.Msg { return editedMsg{err: errors.New("editor が設定されていません（config の editor か環境変数 EDITOR）")} }` を返す `Editor`。それ以外は `os.CreateTemp("", "sugi-loop-answer-*.md")` に `initial` を書き、非公開の `editorCmd(command, path string) *exec.Cmd`（`strings.Fields(command)` の先頭を実行ファイル、残り + `path` を引数）で `*exec.Cmd` を組み、Bubble Tea の外部プロセス実行の関数に、非公開の `readEdited(path string, runErr error) editedMsg`（`runErr != nil` なら `os.Remove(path)` して `editedMsg{err: runErr}`、そうでなければ `os.ReadFile(path)` → `os.Remove(path)` → `editedMsg{text: string(b)}`）を呼ぶコールバックを付けて返す。一時ファイルの作成に失敗したらそのエラーの `editedMsg` を返すコマンドを返す
- [x] 3.2 `internal/ui/editor_test.go` で検証する: `ExternalEditor("")("Q1: A")()` のメッセージは `editedMsg` で `err` の文字列に `editor` を含む。`editorCmd("code --wait", "/tmp/x.md").Args` は `[code --wait /tmp/x.md]`。`os.CreateTemp` で作って `Q1: B` を書いたファイルに `readEdited(path, nil)` → `text` が `Q1: B` でファイルが無い。`readEdited(path, errors.New("exit status 1"))` → `err` がそれでファイルが無い。`ExternalEditor("code --wait")` が作る一時ファイルの名前が `.md` で終わり内容が `initial` であることは、一時ファイルを作る部分を非公開関数に分けて検証する
- [x] 3.3 `internal/ui/model.go` を変える: `New(fetcher Fetcher, client gh.GHClient, editor Editor) Model` にして `client` / `editor` を保持し、回答の状態 `answer{target action.Target; label string; draft string; reason answerReason; from screen}`、`posting bool`、`answerStatus string` と `answerStatusErr bool` を足す。画面の状態に 確認 を足す。`Update` の先頭（画面に関わらず）で `editedMsg` と `postedMsg` を処理する。`answerStatus` は取得が始まるとき（`fetching` を true にする箇所。この change では `New`。後続 change の `R` / 自動更新も同じ箇所を通る）と `a` を押したときに空にする。`fetchedMsg` の処理では触らない。既存の `New(fetcher)` の呼び出し（`model_test.go` / `view_test.go` / `detail_test.go`）を `New(fetcher, gh.NewFake("../gh/testdata/fixtures/example"), スタブ)` に直す
- [x] 3.4 `internal/ui/answer.go` に `a` のキー処理を実装する: `posting` なら `m, nil`。キュー画面は選択行の `row`（`internal/ui/rows.go`。`row.repo` / `row.number` / `row.isPR` で `Target` と表示名、`row.comments` でテンプレート。`subjectOf(row.card)` でも同じ。選択行が無ければ `m, nil`）、カード詳細は `detail.card.Issue`、PR 詳細は `detail.card.PRs[detail.prIdx]` から `Target` と表示名（`<Repo> PR#<n>` / `<Repo> #<n>`）と `Comments` を取り、`answer = {target, label, from: screen}`、`answerStatus = ""` にして `m.editor(action.AnswerTemplate(comments))` を返す。`editedMsg` の処理: `err != nil` → `answerStatus = "エディタ: " + err.Error()`（赤）。`strings.TrimSpace(text) == ""` → `回答を中止しました（本文が空）`。`action.HasRoutineMarker(text)` が真（`action.Comment` と同じ判定関数）→ `draft = text`、`reason` マーカー、`screen` 確認。`len(action.BlockedByLines(text)) > 0` → `draft = text`、`reason` blocked-by、`screen` 確認。それ以外 → 投稿コマンド。投稿コマンドは非公開の `postCmd(client, target, label, body) tea.Cmd`（`context.WithTimeout(context.Background(), 30*time.Second)` で `action.Comment` を呼び `postedMsg{label, err}` を返す）で、返す前に `posting = true`、`answerStatus = "<label> にコメントを投稿中"`。`postedMsg` の処理: `posting = false`、`err == nil` なら `<label> にコメントしました`、そうでなければ `<label> へのコメントに失敗: <err>`（赤）。`cards` / `at` は触らず、取得コマンドを返さない
- [x] 3.5 同ファイルに確認画面のキー処理と `View` を実装する: `y` → `reason` が blocked-by のときだけ `screen = answer.from` にして `postCmd(..., draft)`、マーカーなら `m, nil`。`e` → `screen = answer.from` にして `m.editor(draft)`。`Esc` → `screen = answer.from`、`answerStatus = "回答を中止しました"`。`q` / `ctrl+c` → `tea.Quit`（先頭の共通処理で済む）。他は `m, nil`。`fetchedMsg` は共通処理のまま（`answer` は触らない）。確認画面の `View` は `internal/ui/view.go` の `screen` の振り分けに足し、上から次の順で描く。1 行目は `回答の確認: <label>`。次に理由の行を出す: 理由が blocked-by なら `blocked-by: で始まる行があります（dispatcher はこの行を含む最新コメントを正本にします）` に続けて `action.BlockedByLines(draft)` の各行を、理由がマーカーなら `<!-- routine --> を含む本文は投稿できません（TUI からの投稿は人の発言でなければなりません）` を出す。次に `─` の区切り線、`draft`、フッタ（左は `y 投稿  e 編集に戻る  Esc 中止  q 終了`。理由がマーカーなら `y 投稿  ` を省く。右は s08 のステータス）を描く。理由の行（blocked-by 該当行）も含めて残りの高さで切り、フッタは常に出す
- [x] 3.6 `internal/ui/view.go` のフッタを直す: キュー画面の左を `j/k 移動  1-4/Tab タブ  Enter 開く  a 回答  q 終了`（1 ペインの `p …` は s08 どおり）、カード詳細を `Esc 戻る  Tab PR 選択  Enter PR を開く  x 展開  g PR へ  a 回答  j/k スクロール  q 終了`、PR 詳細を `Esc 戻る  x 展開  g issue へ  a 回答  j/k スクロール  q 終了` にする。右のステータスは `fetching` ならスピナー + `取得中`、そうでなければ `answerStatus`（`answerStatusErr` なら赤 `#B60205`）、それも無ければ s08 の `errText` / `partial`。s08 の `view_test.go` の「`a 回答` を含まない」検証を「含む」に直し、s09 の `detail_test.go` のフッタ検証に `a 回答` を足す。s09 の `model_test.go` の「未実装のキーは何も変えない」から `a` を外す
- [x] 3.7 `internal/ui/answer_test.go` で検証する。共通の準備: `example` の `Result` は `fetch.Fetch(ctx, gh.NewFake("../gh/testdata/fixtures/example"), []string{"org/app"})` で作り（`at` は `2026-09-05T12:04:00+09:00`）、`New` に渡す `fake := gh.NewFake("../gh/testdata/fixtures/example")` は `Result` を作ったものとは別に新しく作る（s07 の `Fetch` は `ViewIssue` を `Calls` に記録するので、同じ `Fake` を使うと `Calls` が空にならない）。`initial` を記録して固定の `editedMsg` を返すスタブ `Editor` を使い、`example` の `Result` または手書き Card を `fetchedMsg` で `Model` に渡す。コマンドは `cmd()` で実行してメッセージを `Update` に渡す。
  - 対象とテンプレート: 主体が PR 131 で `Comments` が `AI` true の `<!-- routine -->\n## Q1. 分けるか\n- 選択肢 A（推奨）: 分ける\n- 選択肢 B: 分けない` 1 件の手書き Card で `a` → コマンドが返り、実行後にスタブの `initial` は `Q1: A`、`answer.target` は `{org/app, 131, true}`。`example` の異常タブで `a` → `nil`、画面キュー。`example` のカード詳細で `a` → 対象 `{org/app, 108, false}`、`initial` 空。同じカードから `Enter` で PR 詳細に入って `a` → `{org/app, 131, true}`、`initial` 空。バックログの issue 140 で `a` → コマンドが返り対象 `{org/app, 140, false}`、`initial` 空
  - 検査と投稿: スタブが `Q1: A` → `a` → 編集完了 → 投稿コマンドが返り、`View` に `org/app PR#131 にコメントを投稿中` があり、この間の `a` は `nil`。投稿コマンドの実行後 `fake.Calls` は 1 件 `CommentPR` / `org/app` / 131 / `Q1: A`、`View` に `org/app PR#131 にコメントしました`、画面キュー、今やるタブに PR 131 の行が残る、`Calls` に `AddLabel` / `RemoveLabel` 無し。スタブが ` \n` → コマンド `nil`、`Calls` 空、`View` に `回答を中止しました（本文が空）`。スタブが `<!-- routine -->\nQ1: A` → 画面 確認、`View` に `<!-- routine --> を含む本文は投稿できません`、`e 編集に戻る`、`Esc 中止` があり `y 投稿` 無し、`Calls` 空。スタブが `Q1: A\n  blocked-by: human` → 画面 確認、`View` に `回答の確認: org/app PR#131`、`blocked-by: で始まる行があります`、`blocked-by: human`、`Q1: A`、`y 投稿`、`e 編集に戻る`、`Esc 中止`、`Calls` 空。スタブが `err: errors.New("exit status 1")` → コマンド `nil`、`Calls` 空、`View` に `エディタ: exit status 1`、画面キュー
  - 確認画面: blocked-by の確認画面で `y` を与えると投稿コマンドが返り、それを実行すると `Calls` は 1 件（`CommentPR`、`Body` は `Q1: A\n  blocked-by: human`）になり、画面はキューになる。`e` → コマンド → 実行後スタブの `initial` は `Q1: A\n  blocked-by: human`、`Calls` 空。`Esc` → 画面キュー、`Calls` 空、`View` に `回答を中止しました`。マーカーの確認画面で `y` → `nil`、画面 確認、`Calls` 空。`example` のカード詳細で `a` → スタブ `Q1: A\nblocked-by: human` → 確認画面 → `Esc` → 画面カード詳細で `detail.card.Issue.Number` が 108
  - 失敗: `*gh.Fake` を埋め込み `CommentPR` が `errors.New("gh pr comment 131 -R org/app --body-file -: exit 1: HTTP 403")` を返す型を `client` にして `Q1: A` を投稿 → `View` に `org/app PR#131 へのコメントに失敗:` と `HTTP 403`、`cards` 不変
  - `queue-screen` の追加 Scenario: `example` の `Result` を渡した `Model` に `a` → スタブが 1 回呼ばれ `fake.Calls`（`New` に渡した新しい `Fake`）は空
  - `card-detail` の MODIFIED（画面の状態が 4 つ）: s09 の `detail_test.go` の画面遷移の検証は変更なしで通ることを確認する

## 4. cmd/sugi-loop の配線

- [x] 4.1 `cmd/sugi-loop/main.go` の `run()` で `ui.New(fetcher, client, ui.ExternalEditor(cfg.Editor))` を渡す。`cfg.Editor` が空でも `run` は失敗させない（`a` を押したときにフッタにエラーが出る）

## 5. 手動確認と最終確認

- [ ] 5.1 手動確認（投稿しない範囲）: 稼働リポジトリ 1 件を設定した状態（V-2）で起動し、今やるタブの `question` 付き PR で `a` を押すと `$EDITOR` が開いてテンプレート（`## Q1.` があれば `Q1: A` の行、無ければ空）が入っていること、本文を空にして閉じるとフッタに `回答を中止しました（本文が空）` が出て GitHub に何も投稿されないこと、`blocked-by: human` の 1 行だけ書いて閉じると確認画面に該当行が出て `Esc` で中止できること、`EDITOR` を空にして起動し `a` を押すと `editor が設定されていません` が赤で出ることを確認する。実際の投稿は行わない（validation-plan.md「書き込み検証は未定」。実施するかは利用者が判断する）
- [x] 5.2 `gofmt -l .` が空で、`go build ./... && go vet ./... && go test ./...` が通ることを確認する（`golangci-lint run ./...` も通す）。`internal/fetch` / `internal/classify` / `internal/gh` / `internal/model` / `cmd/sugi-loop-cli` の既存テストが変更なしで通ることを確認する
