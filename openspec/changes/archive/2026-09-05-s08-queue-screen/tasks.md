## 1. 前提と依存

- [x] 1.1 s07-fetch-cards が実装済みで `internal/fetch`（`Fetch` / `Result{Cards, Errors}` / `CallTimeout`）が `go build ./...` で通ることを確認する。未実装ならこの change を始めない
- [x] 1.2 `go get charm.land/bubbles/v2@latest charm.land/glamour/v2@latest` で go.mod / go.sum に依存を加える。Bubbles の spinner と Glamour の Markdown レンダラの v2 での使い方（型名・関数名）を README で確認し、spec の振る舞いに合わせる

## 2. 行・経過・主体（画面に依存しない純粋関数）

- [x] 2.1 `internal/ui/elapsed.go` に `Elapsed(now, t time.Time) string` を実装する（s06 `cmd/sugi-loop-cli/classify.go` の `elapsed` と同じ規則: 負なら `0m`、1 時間未満 `<m>m`、24 時間未満 `<h>h`、それ以上 `<d>d`。切り捨て）。`internal/ui/elapsed_test.go` で 12 分前 / 3 時間前 / 2 日前 / 未来 → `12m` / `3h` / `2d` / `0m` を検証する
- [x] 2.2 `internal/ui/rows.go` に `Subject(c model.Card) (repo string, number int, isPR bool, title string, updatedAt time.Time)` を実装する。順序は spec `queue-screen`「行の主体は Card.Result を出した Issue または PR」のとおり（`Issue.Result == c.Result` → `PRs[i].Result == c.Result` の最初 → `Issue` → `PRs[0]`）。同じファイルに、`[]model.Card` を `Card.Result.Tab` で 4 タブに振り分け、タブごとに `Priority` 昇順 → 主体 `UpdatedAt` 降順 → `Repo` 昇順 → 番号昇順で `sort.SliceStable` する非公開関数を置く（主体を各行に保持し、`View` で `Subject` を呼び直さない）
- [x] 2.3 `internal/ui/rows_test.go` で `Subject` を検証する。`fetch.Fetch(ctx, gh.NewFake("../gh/testdata/fixtures/example"), []string{"org/app"})` で得た Card のうち、issue 108 の Card を渡すと `org/app` / 131 / true / PR 131 の `Title` と `UpdatedAt` が返り、issue 140 の Card を渡すと 140 / false が返る。手書きの Card（Issue の `Result` が `in-progress` `#5 は進行中`、`PRs[0]` の `Result` が `in-progress` `PR #9 は auto-fix が受け取り中`、`Card.Result` = `PRs[0].Result`）を渡すと 9 / true が返る。同じファイルで振り分けと並びを検証する: `example` の Card を振り分けると 今やる 1 / バックログ 1 / 進行中 0 / 異常 0 になる。手書きで `Priority` 3 の Card 3 枚（5h / 9h / 1d 前）と `Priority` 1 の Card 2 枚（1h / 12m 前）をその順で渡すと、今やるタブが 12m, 1h, 5h, 9h, 1d の順になる

## 3. Model と取得状態

- [x] 3.1 `internal/ui/model.go` に `type Fetcher func(ctx context.Context) (*fetch.Result, error)`、`type Model struct`（`fetcher Fetcher`、`cards []model.Card`、タブ別の行、`tab model.Tab`、`cursor int`、`at time.Time`（ゼロ値 = 未取得）、`fetching bool`、`errText string`、`partial string`、`width` / `height`（初期値 80 / 24）、`showPreview bool`（1 ペイン用）、spinner）、`New(fetcher Fetcher) Model`（`fetching` を true にして返す。初期状態は常に「これから取得する」）、非公開の `fetchedMsg{res *fetch.Result; err error; at time.Time}` を実装する。取得コマンドは非公開関数 `fetchCmd(fetcher Fetcher) tea.Cmd`（`fetcher(context.Background())` を実行して `fetchedMsg{…, time.Now()}` を返す）に分け、`Init` はスピナーの tick と `fetchCmd(m.fetcher)` を `tea.Batch` で返す。スピナーの tick メッセージは `fetching` の間だけ `Update` で次の tick を返し、`fetching` が false なら何も返さない。`Update` の `fetchedMsg`: `err != nil` なら `errText = err.Error()`（`cards` / `at` は触らない）、nil なら `cards = res.Cards` を振り分け直して `at = msg.at`、`partial` は `len(res.Errors) > 0` のとき `詳細取得の失敗 <n> 件: <Errors[0]>`、それ以外は空。取得成功時は `errText = ""`、失敗時は `partial = ""`。取得開始時（`New` と、以後の取得の開始）に両方を空にする（s12 の `R` もこの経路を使う）。どちらも `fetching = false`、`cursor` を現在タブの行数で丸める
- [x] 3.2 `Update` のキー処理を実装する（`tea.KeyPressMsg` の `String()` で判定。s01 の `main.go` と同じ方式）: `j` / `down` → `cursor+1`（上限 行数-1）、`k` / `up` → `cursor-1`（下限 0）、`1`–`4` → タブ、`tab` → 次のタブ（異常 → 今やる）、`q` / `ctrl+c` → `tea.Quit`、`p` → 1 ペインのときだけ `showPreview` を反転。それ以外は `m, nil`。タブ切替後は `cursor` を新しいタブの行数で丸める。端末サイズのメッセージで `width` / `height` を保持する
- [x] 3.3 `internal/ui/model_test.go` で検証する。`example` の `Result` を `fetchedMsg` で渡した Model に対して: `j`×2 / `k` / `down` / `up` で `cursor` が 1, 2, 1, 2, 1（3 行になるよう手書き Card 3 枚を使う）、2 行で `j`×3 → 1、`k`×3 → 0、`2` / `4` / `tab` / `1` / `3` でタブが バックログ / 異常 / 今やる / 今やる / 進行中、`Enter` / `a` / `t` / `o` / `R` / `?` / `v` / `m` / `n` / `s` / `A` / `g` / `/` でコマンド nil かつタブ・`cursor`・`cards` が不変、`q` と `ctrl+c` で `tea.QuitMsg` を生むコマンドが返る、3 行で `cursor` 2 の後に 1 行の `Result` を渡すと `cursor` 0。取得状態: 呼ばれた回数を数える `Fetcher` で `fetchCmd(fetcher)()` を実行 → 1 回呼ばれ、返った `fetchedMsg` を `New` した Model の `Update` に渡すと `cards` が入る（`Init` の返り値はバッチなので実行しない）。`example` の `Result` の後に `errors.New("search issues: gh search issues: exit 1: rate limited")` の `fetchedMsg` → `cards` と `at` が不変で `errText` に `rate limited`。`New` 直後に error → `cards` 空、`at` ゼロ値、`errText` あり

## 4. View（ヘッダ・表・プレビュー・フッタ・1 ペイン）

- [x] 4.1 `internal/ui/view.go` に `View` を実装する。ヘッダ 1 行: `sugi-loop` + 空白 2 + `[1]今やる <n>  [2]バックログ <n>  [3]進行中 <n>  [4]異常 <n>`（タブ間は空白 2。現在タブは太字）+ 右端に右寄せ（左に最低空白 1）で `↻ HH:MM`（`at` がゼロ値なら `↻ --:--`。`at.Format("15:04")`）。表示幅が `width` を超えるなら `[4]` → `[3]` → `[2]` の順に `[n] <n>`（タブ名なし）へ短縮して収まったところで止め、全部短縮しても超えれば `↻ HH:MM` を省き、それでも超えれば行を `width` で切る。表の 1 行を返す関数: `▶`（選択行）または空白 2 / 優先記号（`map[int]string{0: "!!!", 1: "!!", 2: "!", 3: "●", 5: "●"}`、無ければ `-`）4 / `Kind()` 10 / `Repo` 20 / `#<n>` または `PR<n>` 7 / `Title`（残り幅 = `width` − 48。負なら 0 にしてタイトルを出さず、行は `width` で切る。表示幅で切り詰めて `…`）/ `Elapsed(at, updatedAt)` 5 を Lip Gloss の幅指定で並べ、`Kind()` の色（質問 `#D876E3`、方針 `#B60205`、merge `#0E8A16`、todo 候補 ANSI 6、異常 背景 ANSI 3 + 前景 ANSI 0、その他 / 進行中 装飾なし）を行全体に適用する。フッタ 1 行: 左 `j/k 移動  1-4/Tab タブ  q 終了`（1 ペインでは ` p プレビュー` または ` p 一覧` を追加）、右に `fetching` ならスピナー + `取得中`、`errText` があれば赤（`#B60205`）で `errText`、`partial` があれば赤で `partial`。2 ペイン（`width >= 80 && height >= 20`）はヘッダ / 表 / `─` の区切り線 / プレビュー / フッタ、高さ配分は残り（`height - 3`）を半分ずつ（奇数は表に +1）。1 ペインはヘッダ / 表またはプレビュー（`showPreview`）/ フッタ。表もプレビューも割り当てた高さで切る
- [x] 4.2 `internal/ui/preview.go` にプレビューを実装する: 選択行が無ければ `（このタブにはカードがありません）`。あれば 1 行目 `<主体の Body の先頭の空行を除いた最初の行>   labels: <主体の Labels を空白区切り>`（`Labels` が空なら `labels:` を省く。`Body` も空なら 1 行目を出さない。`Card.Result.Summary` は出さない）、次に主体の `Body` を Glamour（幅 = プレビュー幅、スタイル自動）でレンダリングした文字列（エラーなら `Body` そのまま、空なら省略）、次に主体の `Comments` を順に: 本文から routine マーカー行（`strings.TrimSpace` 後の行全体が `<!-- routine -->` または `&lt;!-- routine --&gt;` に一致する行）を落とし、Glamour（幅 = プレビュー幅 − 1）でレンダリングする（エラーなら取り除いた後の本文そのまま）。`AI` なら見出し `AI  HH:MM`（`CreatedAt.In(at.Location())`）とレンダリング後の各行の左端に `▌` を付ける、人なら見出し `<Author>  HH:MM` と本文をそのまま。主体の `Labels` / `Body` / `Comments` は `Subject` の返り値に無いので、行に主体の `*model.Issue` または `*model.PR` への参照を持たせて取る
- [x] 4.3 `internal/ui/view_test.go` と `preview_test.go` で検証する。ANSI エスケープは `charmbracelet/x/ansi` の Strip 相当の関数で除いてから比較する。`example` の `Result`（`at` = `2026-09-05T12:04:00+09:00`）で: ヘッダに `[1]今やる 1` / `[2]バックログ 1` / `[3]進行中 0` / `[4]異常 0` / `↻ 12:04`、`New` 直後は `[1]今やる 0` と `↻ --:--`、フッタに `j/k 移動` / `1-4/Tab タブ` / `q 終了` があり `a 回答` / `m merge` / `Enter 開く` が無い、issue 108 の行に `!!` / `質問` / `org/app` / `PR131` / PR 131 の `Title` がこの順、issue 140 の行に `-` / `todo 候補` / `#140`、長い `Title` が `…` で終わる、2 行で 1 行目だけ `▶`、`UpdatedAt` `2026-09-05T09:00:00Z` の Card で `at` `2026-09-05T12:04:00Z` → 経過 `3h`。プレビュー（Glamour の前後空行で 10 行を超えて切れるため、いずれも幅 120・高さ 40 のサイズメッセージを与えた後に読む）: 今やるタブで 1 行目に `issue #108 の提案。` と `labels: propose question` / `▌AI` で始まる行 / `▌` 付きの `Q1: マイグレーションを分けますか。` があり、`<!-- routine -->` と `&lt;!-- routine --&gt;` が無い、人のコメント（`AI` false、`Author` `user-2`、`Q1: A`）を持つ手書き Card で `user-2` の行と `Q1: A` の行がどちらも `▌` で始まらない、バックログの issue 140（`Labels` 空）で `起動時に設定ファイルが無いと落ちる` があり `labels:` と `▌` が無い、異常タブで `（このタブにはカードがありません）`。取得状態: `Init` 後（コマンド未実行）のフッタに `取得中`、`fetchedMsg` 成功後に `取得中` 無し、失敗後にフッタに `rate limited`（赤の装飾は 5.2 の手動確認で見る）、`Errors` 2 件（先頭 `ViewPR org/app#131: open pr-131.json: no such file`）の `Result` で表 1 行 + フッタに `詳細取得の失敗 2 件: ViewPR org/app#131`。1 ペインを検証する: サイズ 120×40 を与えると `View` に `PR131` と `issue #108 の提案` の両方が出る。60×40 を与えると `PR131` だけが出てフッタに `p プレビュー` があり、ヘッダは `[1]今やる 1  [2] 1  [3] 0  [4] 0` と `↻ 12:04` になる（短縮前は 71 列）。続けて `p` を与えると `issue #108 の提案` だけが出てフッタに `p 一覧` がある。120×15 を与えると `PR131` だけが出る。120×40 で `p` を与えるとコマンドは nil で `View` は変わらない

## 5. cmd/sugi-loop の配線

- [x] 5.1 `cmd/sugi-loop/main.go` を書き換える: `model` 型と Lip Gloss のスタイルを削除し、`run() error` を実装する。順に `config.DefaultPath()` → `config.Load(path)` → `gh.NewClient()` → `client.Check(context.Background())` → `repos := Config.Repos[i].Name の列` → `ui.New(func(ctx context.Context) (*fetch.Result, error) { return fetch.Fetch(ctx, client, repos) })` → `tea.NewProgram(m).Run()`。`main()` は `run()` のエラーを `fmt.Fprintln(os.Stderr, err)` して `os.Exit(1)`。`cmd/sugi-loop/main_test.go` を削除する（同じ Scenario は `internal/ui/model_test.go` が持つ）
- [x] 5.2 手動確認: `go build -o bin/sugi-loop ./cmd/sugi-loop` が通る。`HOME` を空ディレクトリにして実行すると設定ファイルのパスを含むエラーが標準エラーに 1 行出て終了コード 1。`GH_TOKEN=invalid` 等で `gh auth status` を失敗させて実行すると stderr を含むエラーで終了コード 1。稼働リポジトリ 1 件を設定した状態（V-2）で起動すると、キュー画面が `取得中` から Card の並びに変わり、行が種別の色で描かれ、取得失敗のエラーが赤で出て、`j` / `k` / `1`–`4` / `Tab` / `q` が効き、端末を 80 列未満に縮めると 1 ペインになる（設定ファイルと対話端末の要る V-2 の実機起動だけは実施できず未確認。ビルド / 設定エラー / gh 認証エラー / 端末が無い環境でのプログラム実行エラーは確認済み）

## 6. 最終確認

- [x] 6.1 `gofmt -l .` が空で、`go build ./... && go vet ./... && go test ./...` が通ることを確認する（`golangci-lint run ./...` も通す）。`internal/fetch` / `internal/classify` / `internal/gh` / `cmd/sugi-loop-cli` の既存テストが変更なしで通ることを確認する
