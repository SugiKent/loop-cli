## 1. 前提と依存

- [x] 1.1 s12-open-refresh-help と s08a-onboarding が実装済みで、`internal/ui/model.go` に非公開の `startFetch() tea.Cmd`（`fetching` true、`errText` / `partial` / 書き込みステータスを空にし、スピナーの tick と `fetchCmd` を束ねる）、`cmd/sugi-loop/main.go` に `ensureConfig` / `checkError`、`cmd/sugi-loop/main_test.go` があり、`go build ./... && go test ./...` が通ることを確認する。未実装ならこの change を始めない。`openspec/specs/queue-screen/spec.md` の「Model は Card をタブ別に並べ、選択行を 1 つ持つ」「取得は非同期に行い、取得中はスピナー、失敗時は前回結果を維持する」と s08a の `tui-entrypoint`「1 フレーム描画」を、この change の MODIFIED と突き合わせ、本文がレビューで変わっていれば写し直す
- [x] 1.2 `internal/model/model.go` と `internal/gh/types.go` の `Card` / `Issue` / `PR` / `Comment` / `PRMergeState` / `StatusCheck` / `ReviewThread` / `ReviewComment` / `Author` に関数・チャネル・interface 値のフィールドが無いことを確認する。`go.mod` に `github.com/gen2brain/beeep v0.11.2` があり、`cmd/sugi-loop-cli/notify.go` が `beeep.Notify("sugi-loop", "テスト通知", "")` を呼んでいることを確認する

## 2. スナップショット（internal/snapshot）

- [x] 2.1 `internal/snapshot/snapshot.go` を作る。`type Snapshot struct { Cards []model.Card; At time.Time }`、`DefaultPath() (string, error)`（`os.UserHomeDir()` + `.cache/sugi-loop/snapshot.json`）、`Save(path string, s Snapshot) error`（`os.MkdirAll(filepath.Dir(path), 0o700)` → `json.Marshal` → `os.WriteFile(path, data, 0o600)`）、`Load(path string) (Snapshot, error)`（`os.ReadFile` → `json.Unmarshal`。無い・読めない・壊れているは同じくエラー。ファイルを消さない）。`internal/model` 以外に依存しない
- [x] 2.2 `internal/snapshot/snapshot_test.go` で検証する。`HOME` を `t.Setenv` で `/tmp/h` にして `DefaultPath()` が `/tmp/h/.cache/sugi-loop/snapshot.json` を返す。`fetch.Fetch(context.Background(), gh.NewFake("../gh/testdata/fixtures/example"), []string{"org/app"})` の `Cards` と `At` `time.Date(2026, 9, 5, 3, 4, 0, 0, time.UTC)` を `t.TempDir()` の下の未作成ディレクトリ `sugi-loop/snapshot.json` に `Save` → `Load` し `reflect.DeepEqual` で一致し、issue 140 の `Issue.Comments` が `nil` のままで、issue 108 の `PRs[0].ReviewThreads` の `nil` / 非 `nil` が `Save` 前と同じ。`Comments` が `nil` の Card と `[]model.Comment{}` の Card を往復して前者は `nil`、後者は非 `nil` の長さ 0。存在しないパスの `Load` はエラー。内容 `{"cards": [` のファイルの `Load` はエラーで、ファイルの内容は変わらない

## 3. New の拡張と stale 表示（internal/ui）

- [x] 3.1 `internal/ui/model.go` に公開の `Options struct { Snapshot *snapshot.Snapshot; RefreshInterval time.Duration; Notify Notifier }` を足し、`New(fetcher Fetcher, client gh.GHClient, editor Editor, opts Options) Model` に変える。`opts.Snapshot` が非 `nil` なら `cards` / `rows`（`buildRows`）/ `at` をその `Cards` / `At` で初期化する。`fetching` は従来どおり true。`opts.RefreshInterval` と `opts.Notify` を `Model` に保持する。`internal/ui` 内で `New` を呼ぶテストヘルパー（`testdata_test.go` の `newModel`、`answer_test.go` の `answerModel`、`detail_test.go` の `detailModel`、s11 / s12 が足したもの）と `cmd/sugi-loop/main.go` の呼び出しを `Options{}` 付きに直し、`go build ./... && go test ./internal/ui/` が通る状態に戻す
- [x] 3.2 `internal/ui/model_test.go` で stale 表示を検証する。`example` の `Result.Cards` と `At` `2026-09-05T11:50:00+09:00` の `Snapshot` を `Options` で渡して `New` した `Model` の `View`（ANSI を除く）に、今やるタブの PR 131 の行、ヘッダ `[1]今やる 1` と `↻ 11:50`、フッタ `取得中` があり、`routines-setup` が無い。同じ `Model` に issue 140 だけの `Result` を `12:04` で渡すと今やる 0 行・バックログ 1 行・`↻ 12:04`・`取得中` 無し。別の同じ `Model` に `rate limited` の失敗を渡すと PR 131 の行と `↻ 11:50` が残り `rate limited` が出る。`Options{}` で `New` した `Model` は `Init` 後（コマンド未実行）の `View` で表 0 行・`↻ --:--`・`取得中`

## 4. 自動更新（internal/ui/refresh.go）

- [x] 4.1 `internal/ui/refresh.go` を作る。`refreshTickMsg struct{}`、`tickCmd(interval time.Duration) tea.Cmd`（Bubble Tea の遅延コマンドで `interval` 後に `refreshTickMsg{}` を返す）、`Model` の `updateTick() (Model, tea.Cmd)`（`fetching || posting` なら `m, tickCmd(m.refreshInterval)`、そうでなければ `startFetch()` の返り値と `tickCmd` を束ねて返す）。`model.go` の `Init` は `refreshInterval > 0` のとき `startFetch()` の束に `tickCmd` を加え、0 のときは従来どおり。`Update` の `switch` に `refreshTickMsg` のケースを足し、画面の状態を見ずに `updateTick()` を呼ぶ
- [x] 4.2 `internal/ui/refresh_test.go` で検証する。呼ばれた回数を数える `Fetcher` と `Options{RefreshInterval: time.Second}` で `New` し `example` の `Result` を渡した `Model` に `refreshTickMsg{}` を `Update` で渡すと、`fetching` が true で `View` に `取得中` があり、返ったコマンドを実行し（束なら各要素も実行し）得たメッセージに `fetchedMsg` が含まれ `Fetcher` は 1 回呼ばれる（`tickCmd` の要素は実行すると 1 秒待つので、`fetchedMsg` を得た時点で残りは実行しなくてよい）。`New` 直後の `Model` に tick を渡すとコマンドが返り `Fetcher` は 0 回。`example` のバックログの issue 140 に `t` を与えた直後に tick を渡すと `fetching` は false、フッタに `切り替え中` が残り `取得中` は無い。issue 108 のカード詳細を開いて tick を渡し、issue 108 を含まない `Result` を取得完了として渡すと画面はカード詳細のまま、対象は issue 108 で、`Esc` で戻ると今やるタブは新しい行。`Options{RefreshInterval: time.Second}` と `Options{}` の各 `Init` がコマンドを返し `fetching` が true

## 5. 通知（internal/ui/notify.go）

- [x] 5.1 `internal/ui/notify.go` を作る。公開の `type Notifier func(title, body string) error`。非公開の `addedNow(prev, next []model.Card) []model.Card`（`Result.Tab == model.TabNow` の Card だけを対象に、`Subject` の（`repo`, `number`, `isPR`）をキーにした `prev` の集合を作り、`next` の順で集合に無い Card を返す）。`notifyBody(c model.Card) string`（`c.Result.Summary` + `"\n"` + 表示名。表示名は `Subject` の `isPR` で `<Repo> PR#<n>` / `<Repo> #<n>`）。`notifyCmd(notify Notifier, cards []model.Card) tea.Cmd`（各 Card について `notify("sugi-loop", notifyBody(c))` を順に呼び、エラーは無視し、`nil` メッセージを返す）。`model.go` の `fetchedMsg` 成功の分岐で、`cards` を差し替える前に `prev := m.cards`、`hadPrev := !m.at.IsZero()` を取り、差し替え後に `m.notify != nil && hadPrev` なら `added := addedNow(prev, msg.res.Cards)` を求め、1 件以上あれば `notifyCmd(m.notify, added)` を返す（それ以外は従来どおり `nil`）
- [x] 5.2 `internal/ui/notify_test.go` で `addedNow` を検証する。今やるの `org/app PR#131`（主体 PR）と `org/app #91`（主体 Issue）の `prev` に対し、`org/app #91`（`UpdatedAt` を進めたもの）と `org/web PR#88` とバックログの `org/app #140` の `next` で `org/web PR#88` の 1 枚だけ返る。`example` の `Cards` を両方に渡すと長さ 0。`prev` が `nil` で `next` が `example` なら issue 108 の Card 1 枚。主体 PR 131 の issue 108 の Card を `prev`、同じ issue 108 で `Result` を `{Situation: model.SituationB, Priority: 2, Tab: model.TabNow, Summary: …}` にし `Issue.Result` も同じにした Card を `next` にすると 1 枚返る
- [x] 5.3 同ファイルで通知の回数と本文を検証する。`title` / `body` を順に記録するスタブ `Notifier`（`func(title, body string) error` を返すクロージャ）を `Options{Notify: …}` で渡す。issue 140 だけの `Result`（`12:00`）→ `example` の `Result`（`12:04`）の順で渡し、2 回目に返ったコマンドを実行すると記録は 1 件で `title` `sugi-loop`、`body` は `<今やるの行の Summary>\norg/app PR#131`。`Cards` 空 → 今やる 2 枚（`org/app PR#131` と `org/web PR#88`、`Priority` 1）+ バックログ 1 枚の `Result` で記録 2 件、`body` の 2 行目は今やるタブの行の順。スナップショット無しの `New` 直後に `example` を渡すと記録は空（コマンドが返れば実行しても空）。issue 140 だけの `Snapshot`（`At` `11:50`）で `New` して `example` を渡しコマンドを実行すると記録 1 件で 2 行目 `org/app PR#131`。`example` を 2 回 → `Cards` 空の順で渡しても記録は増えない。`Options{}`（`Notify` 無し）で issue 140 → `example` を渡すとどちらもコマンドは返らず `Cards` は `example`。1 回目だけ error `notification failed` を返すスタブで 2 件増える手順を行うと記録 2 件でフッタに `notification failed` は無い。`example` の後に `rate limited` の失敗を渡すと記録は増えず `Cards` は `example` のまま

## 6. cmd/sugi-loop の配線

- [x] 6.1 `cmd/sugi-loop/main.go` に非公開の `savingFetcher(fetcher ui.Fetcher, path string) ui.Fetcher` を足す（`fetcher(ctx)` の `err == nil` なら `snapshot.Save(path, snapshot.Snapshot{Cards: res.Cards, At: time.Now()})` を呼び、その戻り値を無視して `res, nil` を返す。`err != nil` なら保存せず `nil, err`）。`run()` を、`Check` の後に `snapshot.DefaultPath()` → 成功なら `snapshot.Load(path)`（エラーなら `Snapshot` は `nil`）→ `fetch.Fetch` を閉じた `Fetcher` を `path` があれば `savingFetcher` で包む → `ui.Options{Snapshot: snap, RefreshInterval: time.Duration(cfg.RefreshIntervalSec) * time.Second, Notify: notifier}`（`cfg.Notify` が true なら `func(title, body string) error { return beeep.Notify(title, body, "") }`、false なら `nil`）を `ui.New` に渡す順にする。`DefaultPath()` のエラーは起動失敗にせず、読み込みも保存もしない。`main()` と `ensureConfig` / `checkError` は変えない
- [x] 6.2 `cmd/sugi-loop/main_test.go` に `savingFetcher` の検証を足す。`example` の `Result` を返す `Fetcher` を `t.TempDir()` の下の未作成パスで包んで呼ぶと、返り値はその `Result` とエラー nil で、パスを `snapshot.Load` した `Cards` が `reflect.DeepEqual` で等しく、`At` は呼ぶ前後の `time.Now()` の間。`rate limited` の error を返す `Fetcher` を存在しないパスで包んで呼ぶとそのエラーが返りファイルは無い。既存ディレクトリと同じパス（書き込めない）で包んで呼ぶと `Result` とエラー nil が返る

## 7. 手動確認と最終確認

- [ ] 7.1 手動確認: 稼働リポジトリ 1 件を設定（V-2）し `notify: true` で起動する。初回起動でキューが埋まった後に終了し、`~/.cache/sugi-loop/snapshot.json` があること、再起動で前回の表とヘッダの保存時刻が即座に出て `取得中` のスピナーが回り、完了で時刻が更新されることを見る。2 分以上待ってスピナーと `↻` の更新が起きることを見る。別の端末から設定リポジトリの issue に `question` ラベルを付けて（または今やるに増える操作を行って）次の自動更新でデスクトップ通知が 1 件出ることを見る。`notify: false` で通知が出ないことを見る。`snapshot.json` を `{"cards": [` に壊して起動すると空の画面から始まり、取得完了後にファイルが正しい JSON に上書きされることを見る
- [x] 7.2 `gofmt -l .` が空で、`go build ./... && go vet ./... && go test ./...` が通ることを確認する（`golangci-lint run ./...` も通す）。`internal/fetch` / `internal/classify` / `internal/gh` / `internal/model` / `internal/action` / `internal/config` / `internal/onboarding` / `cmd/sugi-loop-cli` の既存テストが変更なしで通ることを確認する
