## 1. 前提と依存

- [x] 1.1 s10-answer-question が実装済みで、`internal/action`（`Target` / `Comment`）、`internal/ui` の `New(fetcher, client, editor)`、フッタ右のステータス、投稿中フラグ、画面の状態（キュー / カード詳細 / PR 詳細 / 確認）があり `go build ./... && go test ./internal/ui/ ./internal/action/` が通ることを確認する。未実装ならこの change を始めない。s10 の最終版の specs と、この change の MODIFIED（`queue-screen` 2 件、`card-detail` 1 件、`answer-question` 2 件）を突き合わせ、本文がレビューで変わっていれば写し直す

## 2. internal/action（stage:todo の切り替え）

- [x] 2.1 `internal/action/testdata/todo/` に `gh issue view --json number,title,body,url,labels,comments` の形の JSON を手書きで置く: `issue-150.json`（`labels` が `stage:todo` と `bug`）/ `issue-151.json`（`stage:propose` と `question`）/ `issue-152.json`（`blocked`）/ `issue-153.json`（空）/ `issue-154.json`（`bug` と `enhancement`）/ `issue-155.json`（`stage:todo` と `stage:propose`）。`comments` は空配列、`title` / `body` / `url` は任意の短い値でよい。`gh.NewFake("testdata/todo").ViewIssue` で各ファイルがデコードできることをテストで確認する
- [x] 2.2 `internal/action/todo.go` にエラー値 `ErrOtherStage` / `ErrMultipleStages` / `ErrBlocked` と `ToggleTodo(ctx context.Context, client gh.GHClient, repo string, number int) (added bool, err error)` を実装する。`client.ViewIssue(ctx, repo, number)` の `Labels[].Name` を `labels` にし、`stages := model.IssueStages(labels)` で順に判定する: `len(stages) >= 2` → `fmt.Errorf("%w（%s）", ErrMultipleStages, strings.Join(stages, " "))`。`model.HasLabel(labels, model.LabelStageTodo)` → `client.RemoveLabel(ctx, repo, number, model.LabelStageTodo)` を呼び `false, err`。`len(stages) == 1` → `fmt.Errorf("%w（%s）", ErrOtherStage, stages[0])`。`model.HasLabel(labels, model.LabelBlocked)` → `fmt.Errorf("%w（blocked）", ErrBlocked)`。それ以外 → `client.AddLabel(ctx, repo, number, model.LabelStageTodo)` を呼び `true, err`。`ViewIssue` のエラーはそのまま返す。`AddLabel` / `RemoveLabel` 以外の書き込みメソッドを呼ばない
- [x] 2.3 `internal/action/todo_test.go` で `gh.NewFake("testdata/todo")` を `client` にして検証する（Scenario ごとに新しい `Fake` を作る）: 153 → `true, nil`、`Calls` は `ViewIssue(org/app, 153)` → `AddLabel(org/app, 153, stage:todo)` の 2 件。150 → `false, nil`、`Calls` は `ViewIssue` → `RemoveLabel(150, stage:todo)`。154 → `true`、2 件目が `AddLabel`。151 → `errors.Is(err, ErrOtherStage)`、`err.Error()` に `stage:propose`、`Calls` は `ViewIssue` 1 件。155 → `ErrMultipleStages`、`err.Error()` に `stage:todo` と `stage:propose`、`Calls` 1 件。152 → `ErrBlocked`、`Calls` 1 件。999（ファイル無し）→ エラー文字列に `issue-999.json`、`Calls` 1 件。全 Scenario を通して `Calls` の `AddLabel` / `RemoveLabel` の `Label` がすべて `stage:todo` で、`CommentIssue` / `CommentPR` / `MergePR` / `CreateIssue` / `ReplyReviewThread` / `Browse` が無いことを確認する

## 3. internal/ui（t のフロー・ステータス・排他）

- [x] 3.1 `internal/ui/todo.go` に `t` のキー処理を実装する: 書き込み中（s10 の投稿中フラグ。名前を `writing` 等に汎用化してよい）なら `m, nil`。対象はキュー画面なら選択行の `row.card.Issue`（行が無い、または nil なら `m, nil`）、カード詳細なら `detail.card.Issue`、PR 詳細と確認画面なら `m, nil`。表示名 `<Repo> #<Number>` を作り、フラグを立て、ステータスを `<表示名> の stage:todo を切り替え中` にして、非公開の `toggleCmd(client, repo, number, label) tea.Cmd`（`context.WithTimeout(context.Background(), 30*time.Second)` で `action.ToggleTodo` を呼び `toggledMsg{label, added, err}` を返す）を返す。`toggledMsg` の処理（`Update` の先頭、画面に関わらず）: フラグを下ろし、`err == nil && added` → `<label> に stage:todo を付けました`、`err == nil && !added` → `<label> から stage:todo を外しました`、`err != nil` → `<label> の stage:todo を切り替えられません: <err>`（赤）。`cards` / `at` / `detail` / `Issue.Labels` は触らず、取得コマンドを返さない。s10 の `a` のキー処理でステータスを空にする箇所と同じく、`t` を押したときも前のステータスを消す（切り替え中の文言で上書きする）
- [x] 3.2 `internal/ui/model.go` の `updateKey`（キュー画面）とカード詳細のキー処理に `t` の振り分けを足し、s10 の `a` の処理の先頭の「投稿中なら何もしない」を共有フラグで判定する。`internal/ui/view.go` のフッタを直す: キュー画面の左を `j/k 移動  1-4/Tab タブ  Enter 開く  a 回答  t todo  q 終了`（1 ペインの `p …` は s08 どおり）、カード詳細を `Esc 戻る  Tab PR 選択  Enter PR を開く  x 展開  g PR へ  a 回答  t todo  j/k スクロール  q 終了`（`PRs` が空なら s09 どおり `Tab PR 選択  Enter PR を開く  g PR へ` を省く）。PR 詳細は変えない。s10 の `model_test.go` の「未実装のキーは何も変えない」から `t` を外し、`view_test.go` / `detail_test.go` のフッタ検証に `t todo`（PR 詳細は「含まない」）を足す
- [x] 3.3 `internal/ui/todo_test.go` で検証する。共通の準備は s10 の `answer_test.go` と同じ（`example` の `Result` は `fetch.Fetch` で作り、`New` に渡す `Fake` は `gh.NewFake` で別に作る。コマンドは `cmd()` で実行してメッセージを `Update` に渡す。`View` は ANSI エスケープを除いて読む）
  - 付ける: `gh.NewFake("../gh/testdata/fixtures/example")` を `client`、`example` の `Result` を渡してバックログタブを選び `t` → コマンドが返り、`View` に `org/app #140 の stage:todo を切り替え中`。実行後 `fake.Calls` は `ViewIssue(org/app, 140)` → `AddLabel(org/app, 140, stage:todo)` の 2 件、`View` に `org/app #140 に stage:todo を付けました`、画面キュー、バックログに issue 140 の行が残り `Issue.Labels` は空のまま
  - 主体が PR の Card: 今やるタブ（issue 108 + PR 131）で `t` → 実行後 `Calls` は `ViewIssue(108)` の 1 件だけ（`AddLabel` / `RemoveLabel` 無し、`Number` 131 の要素無し）、`View` に `org/app #108 の stage:todo を切り替えられません:` と `stage:propose`、`cards` 不変
  - 外す: `gh.NewFake("../action/testdata/todo")` を `client`、`Repo` `org/app` / `Number` 150 / `Labels` `stage:todo` / `Result` `in-progress`（`Tab` 進行中）の issue の Card 1 枚を `fetchedMsg` で渡し、進行中タブで `t` → 実行後 `Calls` の 2 件目が `RemoveLabel(150, stage:todo)`、`View` に `org/app #150 から stage:todo を外しました`
  - カード詳細: `example` のバックログで `Enter` → `t` → 実行後 `Calls` 2 件目が `AddLabel(140)`、画面カード詳細、`detail.card.Issue.Number` が 140
  - 何もしない: `Issue` nil で `docs` PR 60 の Card を今やるに持つ `Model` で `t` → `nil`、`Calls` 空。`example` の issue 108 のカード詳細から `Enter` で PR 詳細を開いた `Model` に `t` を与えると `nil` が返り `Calls` は空。異常タブ（0 行）で `t` を与えると `nil` が返る
  - 失敗: `*gh.Fake` を埋め込み `AddLabel` が `errors.New("gh issue edit 140 -R org/app --add-label stage:todo: exit 1: HTTP 403")` を返す型を `client` にして issue 140 で `t` → `View` に `org/app #140 の stage:todo を切り替えられません:` と `HTTP 403`、`cards` 不変
  - ステータスの寿命: 付けた後にもう一度 `t` → `View` に `付けました` が無く `切り替え中` がある
  - 排他: issue 140 で `t` の直後（コマンド未実行）に `t` → `nil`、`a` → `nil` でスタブ `Editor` は呼ばれない、`Calls` 空。s10 の手順で `a` → 編集完了 → 投稿コマンドが返った直後に `t` → `nil`、`Calls` 空。`toggledMsg` を渡した後にもう一度 `t` → コマンドが返る
  - `queue-screen` / `card-detail` の MODIFIED: s10 の `model_test.go` の「未実装のキー」から `t` を外した検証と、フッタの `t todo` の検証（キュー・カード詳細は含む、PR 詳細は含まない）が通る
  - `answer-question` の MODIFIED: 上の「排他」（切り替え中の `a` が `nil`、投稿中の `t` が `nil`）と、投稿完了のステータス `コメントしました` が次の `t` で消えることを確認する

## 4. 手動確認と最終確認

- [ ] 4.1 手動確認: 稼働リポジトリ 1 件を設定した状態（V-2）で起動し、キュー画面とカード詳細のフッタに `t todo` があり、PR 詳細には無いこと、`stage:propose` 付き issue の Card で `t` を押すとフッタに `切り替えられません:` と `stage:propose` が赤で出て GitHub のラベルが変わらないこと（`gh issue view <n> -R <repo> --json labels` で確認）を確認する。ラベル無しの issue への実際の付与は行わない（validation-plan.md「書き込み検証は未定」。実施するかは利用者が判断する。実施する場合は `t` → `gh issue view … --json labels` で `stage:todo` が 1 つ付いたこと → `t` → 外れたことを確認する）
- [x] 4.2 `gofmt -l .` が空で、`go build ./... && go vet ./... && go test ./...` が通ることを確認する（`golangci-lint run ./...` も通す）。`internal/fetch` / `internal/classify` / `internal/gh` / `internal/model` / `cmd/sugi-loop-cli` の既存テストが変更なしで通ることを確認する
